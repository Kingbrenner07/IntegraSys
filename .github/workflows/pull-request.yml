name: Validate dashboard pull request

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: dashboard-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  dashboard-validation:
    name: Dashboard validation
    runs-on: ubuntu-latest
    env:
      BASE_PATH: /IntegraSys/
      PORT: "4173"
      VITE_SUPABASE_URL: https://example.supabase.co
      VITE_SUPABASE_ANON_KEY: pull-request-build-placeholder

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck dashboard
        run: pnpm --filter @workspace/rh-dashboard run typecheck

      - name: Build dashboard
        run: pnpm --filter @workspace/rh-dashboard run build
